<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="PhantasmaMail.Controls.MailFrameCell"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentView.Content>
        <Grid>
            <Frame Padding="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="1" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Text="{Binding Subject}" />
                    <Label Grid.Row="1" Text="{Binding Content}" />
                    <BoxView Grid.Row="2" BackgroundColor="Black" />
                    <Label Grid.Row="3" Text="{Binding FromName}" />
                </Grid>
            </Frame>
        </Grid>
    </ContentView.Content>
</ContentView>